# Логи и временные файлы
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

# Кэш зависимостей и артефакты сборки
node_modules
dist
dist-ssr
*.local

# Файлы IDE
.idea
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Файлы операционной системы
.DS_Store
Thumbs.db

# Переменные окружения (ОЧЕНЬ ВАЖНО!)
.env
.env.*
!.env.example
!.env.template

# Специфичные для проекта файлы
# Файл с кэшем ID Shikimori (из server.js)
server/available_shiki_ids.json

# Артефакты сборки клиента Vite (на всякий случай, если .gitignore клиента не сработает)
client/dist
client/.vite
client/coverage

# Файлы тестов (если будут)
coverage
.nyc_output

# Локальные настройки редакторов
.eslintcache
.prettierrc.local

# Другие временные файлы
*.tsbuildinfo